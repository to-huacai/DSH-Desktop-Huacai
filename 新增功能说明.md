# DSH-Desktop-Huacai 1.14 新增功能说明

本次桌面版把 dsh 打包成了**一个免安装、免依赖的 exe**,并内置了五大功能
(皮肤、归档、一键更新、编辑器模式、**终端**)。下面是和旧方式(装 Node.js → 敲命令 → 旧启动器)相比,
这次新增/改进的全部内容。

---

## 一、单文件自包含:装好就能用

| | 旧方式 | 本次桌面版 |
| --- | --- | --- |
| 安装 | 三步:① 装 Node.js ② 手动执行 `npx @deepseek-ai/dsh web` ③ 双击旧启动器 | **一步:双击一个 exe** |
| 依赖 | 目标电脑必须装 Node.js | **不需要装任何东西** |
| 联网 | 启动依赖网络下载 | **全程离线可用**(内置 Node 运行时 + 完整 dsh 应用) |
| 分发 | 多个文件/步骤 | **单文件**,复制到任意 Windows 10/11 电脑双击即用 |

首次运行自动解压(约 260MB,只需一次),之后**秒开**。

## 二、新增内置功能一:皮肤(浅色主题)

- **入口**:设置(左下角)→ **皮肤**
- **功能**:
  - 一键启用浅色主题,5 套预设主色调(粉/蓝/绿/紫/橙)+ 自定义取色器
  - 可选**照片背景**(内置默认背景图,或上传本地图片 ≤15MB)
  - 半透明面板透明度调节、AI 回复毛玻璃卡片
- **细节**:设置自动保存(`~/.dsh/dsh-skin.json`),刷新/重启都保留

## 三、新增内置功能二:归档(归档会话管理)

- **入口**:侧边栏底部 **「归档」**按钮(设置按钮上方)
- **功能**:
  - 集中查看所有已归档会话(标题、工作区、更新时间、运行中状态),实时联动刷新
  - **恢复**:一键把归档会话移回侧边栏原位置
  - **彻底删除**:确认后删除存档,不可恢复(运行中的会话不可删,有双重保护)
- **细节**:与 dsh 原生归档/恢复/删除操作实时同步,删错可重试、不破坏账目

## 四、新增内置功能三:应用内一键更新

- **入口**:设置 → 通用设置 → 底部 **「DeepSeek Harness 更新」**
- **功能**:
  - **检查更新**:对比当前版本与官方最新版本(npmjs 优先,npmmirror 兜底)
  - **一键更新**:发现新版点一下,自动完成 下载 → 安装 → 重启,**不用重新下载 exe**
  - 失败自动回滚旧版本,数据(会话/密钥/设置)全程不受影响
- **细节**:离线电脑仍可正常使用,只是无法在线更新

## 五、新增内置功能四:编辑器模式(1.10,1.11 增强;1.14 行号 + 图片)

- **入口**:侧边栏底部(设置上方)**「编辑器模式」**按钮,随时与默认对话模式互切
- **布局**(编辑器模式下):
  - 左侧:**文件树** —— 当前工作区的目录/文件,目录可展开折叠,点击文件打开
    - **1.14 新增:图片文件显示在树中**(png/jpg/gif/webp/bmp/ico/avif,粉色色点标识)
  - 中间:**可编辑文件** —— 选中文件的内容,直接编辑,`Ctrl+S` 或「保存」按钮写回磁盘
    - **1.14 新增:行号** —— 编辑器左侧固定行号栏,随滚动/编辑实时同步,行数多时自动加宽
    - **1.14 新增:图片预览** —— 点击图片文件后显示只读预览(透明区域棋盘格底,保存按钮禁用)
  - 右侧:**对话** —— 与默认模式完全一致的对话区,可照常和 agent 交流
  - **右侧顶部(1.11 新增):会话切换条** —— 显示当前会话,点击弹出**完整会话列表**(按最近更新排序,会话多时可搜索),选中即切换,无需左右滑动;「+ 新会话」可在当前项目新建会话
- **细节**:
  - 模式选择自动记忆(刷新/重启界面都保留)
  - 文件树顶部可切换工作区,显示当前正在编辑的文件路径
  - 编辑内容有「未保存」标记;切换文件时若有未保存修改会先确认
  - **语法高亮**:编辑器支持 30+ 常见语言(JS/TS/Python/Rust/Go/Java/C/C++/C#/SQL/HTML/CSS/Shell/YAML 等)的关键字、字符串、数字、注释、函数调用着色,浅色/深色主题自适应;超大文件(>150KB)自动关闭高亮保证输入流畅
  - 大文件(>2MB)不显示在树中;图片(1.14 起)会显示并支持只读预览;node_modules/.git/dist 等目录自动跳过
  - 编辑器只读写当前工作区目录内的文件,路径做了越界防护

## 六、新增内置功能五:终端(1.12 新增,内置面板;1.13 增加终端列表)

- **入口**:侧边栏底部(设置上方)**「终端」**按钮,**对话模式与编辑器模式下都能看到**
- **功能**:
  - 点击后界面底部弹出**内置终端面板**(Qoder 式),直接在页面里敲命令:
    - **PowerShell / CMD 一键切换**(面板头部下拉框);彩色输出、中文宽字符、滚轮回看历史
    - 会话常驻:关闭面板会话继续运行,重新打开自动恢复;「重新启动」可强制重启会话
    - 「外部终端」可改在系统终端窗口(Windows Terminal/cmd)打开当前工作区目录
  - **终端列表(1.13 新增,参考 VS Code)**:面板顶部 Tab 栏,每个 Tab 一个独立终端会话
    - **「+」新建终端**、点 Tab 切换、点 Tab 上 **「×」关闭并终止**该会话;关闭最后
      一个 Tab 自动收起面板;Tab 标题如 `PowerShell`、`CMD`、`PowerShell 2`,悬停显示路径
    - 关闭面板不终止会话,重开面板自动恢复 Tab 列表与各会话输出
  - 打开目录规则(1.13 起跟随**当前选择的项目**):编辑器模式 → 文件树当前项目根目录;
    对话模式 → **当前会话所属工作区**(切换会话/项目后新开的终端跟随新项目;无工作区时退回 DSH 目录/用户主目录)
  - **光标贴合提示符(1.13 修复)**:PowerShell 显示 `PS 目录名>`,cmd 显示 `C>`,光标紧贴
    提示符末尾,不再出现「光标与路径之间的大段空白」(修复 emulator 对 `CSI K/J` 无参数
    erase 的默认值错误,以及 PSReadLine 尾随空格导致的偏移)
- **实现**:内置终端由 **ConPTY(node-pty,随 dsh 应用内置)+ WebSocket 实时中继**驱动,零新增依赖;
  浏览器只负责渲染,shell 跑在 dsh 服务进程里,会话不随页面刷新/面板开关而中断;
  1.13 起服务端按 `sid` 维护多会话 Map(上限 8 个),每个 Tab 独立 shell/目录/历史
- **细节**:打开目录由后端从工作区注册表解析(不接受浏览器传的任意路径),与编辑器文件 API 同一套越界防护思路;HTTP 接口支持 `dryRun` 校验模式,维护者可用它做无窗口的端到端自检

## 七、启动体验改进

- **立即弹窗**:双击后马上出现"初始环境配置中..."窗口 + 进度条,不再黑屏干等
- **加载遮罩**:桌面端(浏览器界面)尚未打开前,启动器窗口会显示"加载中..."遮罩层,
  就绪并打开桌面端后自动消失
- **全自动引导**:解压 → 创建配置 → 自动装齐皮肤/归档/更新插件 → 启动服务 → 打开界面
- **托盘驻留**:关掉窗口服务不中断;右键托盘图标可"打开界面"或"停止并退出"
- **启动自愈**:每次启动全量检查内置插件,发现缺哪个自动补装;**版本不一致也自动覆盖升级**
  (修复了此前 "皮肤在、归档缺失" 以及 "旧版插件不随新版 exe 升级" 的问题)

## 八、功能入口一览

| 功能 | 在哪里 | 一句话 |
| --- | --- | --- |
| 皮肤 | 设置 → 皮肤 | 浅色主题、主题色、背景图、半透明面板 |
| 归档 | 侧边栏底部「归档」 | 查看/恢复/彻底删除归档会话 |
| 更新 | 设置 → 通用设置 | 检查官方新版,一键在线更新,自动重启 |
| 编辑器模式 | 侧边栏底部「编辑器模式」 | 文件树 + 可编辑文件 + 对话(顶部横向会话列表可切换),三栏布局,可随时切回对话模式 |
| 终端(1.12 新增;1.13 加终端列表) | 侧边栏底部「终端」 | 内置终端面板(PowerShell/CMD),1.13 起 VS Code 式多终端 Tab;也可在系统终端打开当前项目目录 |
| 托盘 | 系统托盘图标 | 打开界面 / 停止并退出 |

---

> 给最终用户的日常使用说明见 `使用说明.md`;皮肤/归档的详细操作与维护者重建步骤见 `更新文档.md`。
