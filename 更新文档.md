# DSH-Desktop-Huacai 功能与更新文档(1.11)

本文档说明 DSH-Desktop-Huacai-1.11.exe 自带的**四大功能**(皮肤、归档、更新、编辑器模式)的使用方法,
以及维护者如何跟随官方更新重新构建 exe、更新链路的内部实现与常见问题。

---

## 一、功能总览

| 功能 | 入口 | 作用 | 更新方式 |
| --- | --- | --- | --- |
| 🎨 皮肤 | 设置 → **皮肤** | 浅色主题:主色调、照片背景、半透明面板、回复卡片 | 随 exe(重新构建) |
| 📦 归档 | 侧边栏底部 **「归档」**按钮 | 归档会话的可视化管理:查看、恢复、彻底删除 | 随 exe(重新构建) |
| 🔄 更新 | 设置 → 通用设置 → **「DeepSeek Harness 更新」** | 检查并一键在线更新 dsh 核心 | **应用内一键更新**,无需重装 exe |
| 📝 编辑器模式 | 侧边栏底部 **「编辑器模式」**按钮 | 文件树 + 可编辑文件 + 对话,三栏布局,与默认对话模式互切 | 随 exe(重新构建) |

四个功能都是 exe 内嵌的 @local 插件,首次启动自动装入 profile
(`~/.dsh/profiles/web/node_modules/@local/` + `cordis.patch.yml` 组合行),
收到 exe 的用户**开箱即用**。更新只动 dsh 核心;数据(`~/.dsh` 会话/工作区/密钥/设置)
和这四个插件均不受影响。

---

## 二、皮肤(浅色主题)

**入口:设置(左下角)→ 皮肤**。为 dsh web 提供浅色主题美化,与深色主题
(`data-ds-dark-theme`)互不干扰。

### 设置项

| 设置项 | 说明 |
| --- | --- |
| 启用皮肤 | 开关,关闭后恢复默认外观 |
| 主题颜色 | 5 个预设(粉 `#DB2777` / 蓝 `#4176E6` / 绿 `#16A34A` / 紫 `#7C3AED` / 橙 `#EA580C`)+ 自定义取色器;配套的悬停色、气泡色、面板底色自动衍生 |
| 回复卡片 | 是/否;开启后 AI 回复呈毛玻璃卡片(半透明白 + 背景模糊) |
| 面板透明度 | 滑杆 0.15–0.95,控制侧边栏/面板等半透明程度 |
| 背景图 | 选择本地图片(≤15MB)作浅色模式背景;可移除。未自定义时自动使用内置默认背景图(`/skin-bg`,随 exe 打包) |

### 持久化

所有设置保存在 `~/.dsh/dsh-skin.json`(文件),**刷新页面、重启应用都保留**;
背景图默认值自动发现、不写入文件,用户自定义图片以 dataURI 持久化。

### 更新方式

皮肤插件内嵌在 exe 里,修改皮肤需要**重新构建 exe** 分发(见第五节);
应用内一键更新不会改变皮肤。

---

## 三、归档(归档会话管理)
**入口:侧边栏底部「归档」按钮**(设置按钮上方)。为 dsh web 补上归档会话的可视化管理。

### 功能

- **查看**:面板列出全部已归档会话(标题、所属工作区、更新时间、运行中/空白状态、数量),
  数据来自运行时快照,与别处归档/恢复/删除操作**实时联动**自动刷新
- **恢复**:一键把会话移出归档集合,立即回到侧边栏原工作区位置(幂等)
- **彻底删除**:输入会话标题确认后,删除该会话的 JSONL 存档目录 + 工作区账目
  (归档标记 + workspace 槽位),**不可恢复**

### 边界与安全

- **运行中/打开中的会话不可删除**(前端禁用 + 后端 409 二次校验)
- **删除失败不改账目**:存档目录删除失败时立即中止(500),会话不会"复活"到侧边栏,重试即可
- 搜索索引自愈:`dsh-session-query-sqlite` 下次对账自动清理已删会话,投影缓存残留无害
- 按目录删除依赖默认 jsonl 持久化后端;其他后端会返回明确错误(恢复功能不受影响)
- 插件与 dsh 同进程、直接改写 registry 并删除存档目录,仅建议在可信本机使用

### 更新方式

归档插件内嵌在 exe 里,升级需要**重新构建 exe** 分发(见第五节);
卸载/重装插件不影响已归档数据。

---

## 四、编辑器模式(文件树 + 编辑 + 对话)

**入口:侧边栏底部「编辑器模式」按钮**(设置上方),点击后界面切换为三栏布局,可随时切回默认对话模式。

### 使用

- **左侧 = 文件树**:当前工作区的目录/文件,目录可展开/折叠;顶部下拉框可切换工作区
- **中间 = 编辑器**:点击文件后在中间打开,直接编辑,`Ctrl+S` 或「保存」写回磁盘
  - 有未保存修改时显示「未保存」标记;切换文件会先确认是否放弃修改
- **右侧 = 对话**:与默认模式完全一致的对话区,照常与 agent 交流
  - 1.11 起,对话区顶部有**会话切换条**:显示当前会话 + 会话数,点击弹出**完整会话列表**(按最近更新排序、会话多时可搜索),选中即切换;「+ 新会话」可在当前项目新建会话
- 模式选择保存在浏览器 `localStorage`,刷新/重启界面后保持

### 边界与安全

- 文件树自动跳过 `node_modules` / `.git` / `dist` / `_build` 等目录以及二进制/图片文件;
  目录深度与条目总数有上限,超大目录会提示「仅显示部分文件」
- 文本读取上限 2MB,更大的文件提示在外部编辑
- 插件只解析当前工作区(`workspaceRegistry` 登记路径)内的文件;`..` 越界、绝对路径一律拒绝
- 支持新建文件(写入不存在的路径会自动创建目录);写回为 UTF-8

### 更新方式

编辑器插件内嵌在 exe 里,升级需要**重新构建 exe** 分发(见第五节)。

---

## 五、更新(DeepSeek Harness 更新)

**入口:设置 → 通用设置 → 底部「DeepSeek Harness 更新」**。适用:**通过
DSH-Desktop-Huacai-1.11.exe 启动**的电脑(内嵌版)。

### 操作步骤

1. 打开界面 → 左下角**设置** → **通用设置**
2. 底部「DeepSeek Harness 更新」行:
   - **检查更新**:显示「当前 x.y.z · 最新 x.y.z」
   - 有新版时出现绿色**「一键更新」**按钮
3. 点击后自动完成:下载 → 用内置 npm 安装 → 重启服务 → 浏览器自动重开
   - 新版本生效后,同一行会显示「已是最新版本」

### 注意事项

- 需要电脑能联网访问 npm registry(检查与下载都走网络)
- 更新失败会自动**回滚原版本**(安装前有备份)
- 非内嵌版启动方式(如手动 `npx @deepseek-ai/dsh web`)只显示检查,不提供一键更新
- 更新的是 dsh 核心;**皮肤/归档等插件和启动器本身的升级必须等新版 exe**

### 常见问题

| 现象 | 原因与处理 |
| --- | --- |
| 「检查失败:无法连接 npm 官方源」 | 网络不通或 registry 被墙;检查逻辑会自动换 npmmirror,仍失败说明本机完全无法访问 npm,只能等新版 exe |
| 更新后界面自动重开,版本没变 | 一般不会发生;若出现,看 `%LOCALAPPDATA%\DSH-Desktop-Huacai\logs\launcher.log` 里 `更新失败`/`更新成功` 字样 |
| 想手动确认当前版本 | 设置页该行「当前」值,或看 `app\node_modules\@deepseek-ai\dsh\package.json` 的 `version` |

---

## 六、维护者:官方更新后重新构建

当 npm 上 `@deepseek-ai/dsh` 发布新版本时,把新核心打进 exe 重新分发(皮肤/归档/编辑器插件
的修改也走同样流程):

### 前置条件(构建机)

- 本机有 Node.js(构建脚本用;目标电脑不需要)
- 本机有 .NET Framework 4.x(`C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe`)

### 步骤

```powershell
# 0) 工作目录: to-deepseek
cd C:\Users\<你>\Desktop\to-deepseek

# 1) 拉最新 dsh 到本机 npx 缓存(构建脚本会从这里拷;也可用 -FreshApp 直接 npm 装)
npx -y @deepseek-ai/dsh@latest

# 2) 关键:删除上次构建的 app,否则 build.ps1 会复用旧 dsh(不更新!)
#    (等价做法:构建时加 -FreshApp 参数)
Remove-Item _build\app -Recurse -Force

# 3) 关键:payload 内容变化时,提升版本标记,让已解压的电脑强制重新解压
#    src\Launcher.cs 第 49 行: EMBEDDED_VERSION = "21" → "22"
#    (只有启动器逻辑变化、payload 没变时,可以不改)

# 4) 构建
powershell -ExecutionPolicy Bypass -File build.ps1 -OutExe DSH-Desktop-Huacai-1.11.exe
#    输出: DSH-Desktop-Huacai-1.11.exe (约 118MB), 日志里有 "Embedded dsh version: x.y.z"

# 5) 回归验证(可选)
powershell -ExecutionPolicy Bypass -File run-test.ps1   # 全新环境跑通四插件+服务启停

# 6) 分发新的 DSH-Desktop-Huacai-1.11.exe
```

### 版本标记机制(为什么第 3 步重要)

启动器用三个标记文件判断是否重新解压,内容都是 `EMBEDDED_VERSION`:

```
%LOCALAPPDATA%\DSH-Desktop-Huacai\.embedded
%LOCALAPPDATA%\DSH-Desktop-Huacai\runtime\.dsh-version
%LOCALAPPDATA%\DSH-Desktop-Huacai\app\.dsh-version
```

标记一致 → **跳过解压**(秒开);不一致 → 重新解压。所以:

- **内嵌 dsh 版本变了(新的 app.zip)→ 必须提升 EMBEDDED_VERSION**,否则已解压的电脑
  会继续跑旧核心,看起来"更新了"其实没有
- 只改启动器 C# 逻辑 → 不必提升(逻辑在 exe 本体里,换 exe 即生效)

### 常见问题

| 现象 | 原因与处理 |
| --- | --- |
| 构建日志 `Embedded dsh version` 还是旧版本 | `_build\app` 存在被复用;删掉它或加 `-FreshApp` 重跑 |
| 分发给用户后版本没变 | `EMBEDDED_VERSION` 没提升,旧机器跳过解压;提升后重发 |
| 只想让某一台电脑强制重解压 | 删除该机 `%LOCALAPPDATA%\DSH-Desktop-Huacai` 文件夹后重新双击 exe |

---

## 七、更新链路内部实现(排查用)

### 应用内更新时序

```
浏览器(设置→通用设置)
  │  POST /dsh-updater/update
  ▼
@local/dsh-updater (node half, 跑在 dsh web 进程内)
  │  写入 %LOCALAPPDATA%\DSH-Desktop-Huacai\update-request.json
  │  约 0.8s 后 process.exit(0) —— web 进程退出
  ▼
DSH-Desktop-Huacai 启动器(StartServerMonitor 监控线程)
  │  发现服务退出 + update-request.json 存在
  │  运行: runtime\node.exe apply-update.mjs <request>
  ▼
apply-update.mjs(内嵌 npm)
  │  1) 备份当前 dsh 包 → update-backup\dsh
  │  2) npm install @deepseek-ai/dsh@<目标版本>(npmmirror 优先, npmjs 兜底)
  │  3) 校验安装版本;失败则恢复备份、请求文件留 error
  │  4) 成功则请求文件改名 update-done-<版本>.json
  ▼
启动器重启服务(StartServer) → 浏览器自动重开 → 新版本生效
  (新服务仍在启动器进程树内,托盘"停止并退出"照常工作)
```

### 相关文件

| 文件 | 作用 |
| --- | --- |
| `dsh-bundle\plugin\@local\dsh-updater\lib\index.js` | 检查/更新 API(`/dsh-updater/check`、`/dsh-updater/update`) |
| `dsh-bundle\plugin\@local\dsh-updater\lib\client.js` | 设置页更新行 UI |
| `dsh-bundle\apply-update.mjs` | 备份 → npm 安装 → 校验 → 回滚 |
| `src\Launcher.cs` | `StartServerMonitor`(监控服务退出)、`ApplyUpdateAndRestart`(接棒执行)、`RestartServerAfterUpdate`(重启) |
| `build.ps1` | 构建;runtime.zip 打包 npm(更新用);叠加 `dsh-bundle\` 进 payload |

### 网络与源

- 版本检查:npmjs 优先 → npmmirror 兜底
- npm 安装:npmmirror 优先 → npmjs 兜底(国内网络友好)
- npm 缓存目录:`%LOCALAPPDATA%\DSH-Desktop-Huacai\npm-cache\`

---

## 八、边界与提醒

- **离线电脑**无法应用内更新(需要 npm registry),只能靠新 exe 分发
- **应用内更新 ≠ 插件更新**:皮肤/归档/更新插件本身、以及启动器功能,都随 exe 版本走
- 更新过程约 1-3 分钟(取决于网速);期间界面会断开并自动重开,属正常
- 若启动器日志出现 `更新失败` 且服务未能重启,手动双击 exe 即可恢复(回滚后旧版本仍可用)
