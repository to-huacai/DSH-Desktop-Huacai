# DSH-Desktop-Huacai 自包含版(1.11)— 使用说明

## 解决了什么问题

旧流程需要三步:① 安装 Node.js → ② 手动执行 `npx @deepseek-ai/dsh web` → ③ 双击旧版启动器。

新方案只需要**一步**:

> 双击 `DSH-Desktop-Huacai-1.11.exe`

- **内置 Node.js** —— exe 里打包了便携版 Node 运行时和完整的 dsh 应用,目标电脑**不需要安装 Node**,全程离线可用
- **立即弹窗** —— 双击后马上出现"初始环境配置中..."窗口 + 进度条,不会黑屏干等
- **加载遮罩** —— 桌面端(浏览器界面)打开前,启动器窗口显示"加载中..."遮罩层,就绪后自动消失
- **全自动启动** —— 自动解压内置运行时 → 引导 profile → 安装皮肤/归档/更新插件 → 启动 dsh web → 打开浏览器界面
- **应用内更新** —— 设置 → 通用设置 底部有「DeepSeek Harness 更新」:一键检查 npm 官方新版本,发现新版可在线更新,自动重启生效(无需手动重装 exe)
- **托盘驻留** —— 关掉窗口服务不中断;右键托盘图标可"打开界面"或"停止并退出"

## 使用方法

1. 把 `DSH-Desktop-Huacai-1.11.exe` 复制到任意 Windows 10/11 电脑(无需安装任何东西)
2. 双击运行
3. 首次运行会显示解压进度(内置运行时约 260MB,解压到
   `%LOCALAPPDATA%\DSH-Desktop-Huacai\`,只需一次,之后秒开)
4. 服务就绪后自动打开界面窗口;关闭窗口会最小化到托盘,服务继续运行

> 提示:若 SmartScreen 提示"未知发布者",点"更多信息 → 仍要运行"。
> 未签名 exe 首次运行都会有此提示。
> 从 1.0 升级:直接双击新的 1.11 exe,自动重新解压并装入新插件(皮肤/归档/更新/编辑器),
> 你的会话、工作区、设置全部保留。

## 可选配置 `launcher.json`

在 exe 同目录放一个 `launcher.json` 可覆盖默认值(不放则用默认):

```json
{
  "port": 3080,
  "openBrowser": true,
  "appMode": true,
  "browserExe": null,
  "dshHome": null
}
```

| 字段 | 说明 |
| --- | --- |
| `port` | dsh web 端口,默认 3080 |
| `openBrowser` | 就绪后是否自动打开界面,默认 true |
| `appMode` | 是否用浏览器"应用窗口"模式(无地址栏),默认 true;false 则用系统默认浏览器 |
| `browserExe` | 指定浏览器 exe 完整路径(不填自动找 Chrome/Edge 等) |
| `dshHome` | 指定 DSH_HOME 目录(默认 `~/.dsh`) |

## 应用内更新(内置更新检查)

打开界面 → **设置(左下角)→ 通用设置**,底部有「DeepSeek Harness 更新」:

- **检查更新**:对比当前内嵌版本与 npm 官方最新版(`registry.npmjs.org`,失败自动换 npmmirror)。
- **一键更新**:发现新版时点击,自动完成 下载 → 安装(用内置 npm 更新 `app\node_modules\@deepseek-ai\dsh`)→ 重启服务,浏览器自动重开;失败会自动回滚到原版本。

> 更新需要目标电脑能访问 npm registry;更新的是 dsh 核心,皮肤/归档等插件与你的数据(`~/.dsh`)不受影响。内嵌版以外的启动方式(如直接 `npx dsh web`)只支持检查,不提供一键更新。
>
> - 本次桌面版新增了什么功能:**`新增功能说明.md`**
> - 完整更新机制、维护者重新构建步骤与常见问题:**`更新文档.md`**

## 常用内部参数(测试用)

`DSH-Desktop-Huacai-1.11.exe --skin-install-only` —— 只安装皮肤/归档/更新插件,不启动服务。

## 重新构建(更新 dsh 版本后)

```powershell
# 在 to-deepseek 目录执行(本机需要 Node.js)
powershell -ExecutionPolicy Bypass -File build.ps1 -OutExe DSH-Desktop-Huacai-1.11.exe
```

脚本会:提取上一版 exe 的内嵌插件(自举构建)→ 叠加 `dsh-bundle\`(更新/编辑器插件/脚本)→ 打包内置 Node 运行时(含 npm)→ 从 npx 缓存复制最新 dsh(或 `-FreshApp` 走 npm)→ 编译启动器 → 组装新的 exe。

> 构建前记得提升 `src\Launcher.cs` 里的 `EMBEDDED_VERSION`(payload 变化时必须),否则已解压的电脑会跳过解压、继续用旧插件。

构建产物在 `_build\`(可删),缓存下载在 `build-cache\`(可删)。

## 文件一览

| 文件 | 说明 |
| --- | --- |
| `DSH-Desktop-Huacai-1.11.exe` | 自包含启动器(分发这个就行) |
| `src\Launcher.cs` | 启动器 C# 源码(WinForms, .NET Framework 4.x) |
| `build.ps1` | 一键构建脚本 |
| `dsh-bundle\` | 附加内容:更新/编辑器插件、apply-update.mjs、安装脚本(构建时叠加进 payload) |
| `tools\pack-exe.mjs` | 覆盖层打包(兼容 DSHPAYLD 格式) |
| `tools\zipdir.cs` / `tools\icon-gen.cs` | 构建辅助工具 |
| `parse-payload.mjs` | 覆盖层解析工具 |

## 技术要点

- 覆盖层格式与旧版完全一致(尾部 `DSHPAYLD` + 长度),旧工具可继续解析
- 新覆盖层新增两个大条目:`runtime.zip`(node.exe + LICENSE + npm)和 `app.zip`(完整 dsh 安装)
- 版本标记 `.embedded`/`runtime\.dsh-version`/`app\.dsh-version` 一致时跳过解压,秒开
- 服务进程由启动器托管:关闭即 `taskkill /T` 结束进程树,不留孤儿进程
- 若 exe 同目录存在 `runtime\` 和 `app\` 文件夹(开发模式),优先直接使用,免解压
